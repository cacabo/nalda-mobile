import { StyleSheet } from 'react-native';
import { white, red } from '../colors';

export default StyleSheet.create({
  error: {
    backgroundColor: red,
    padding: 12,
    color: white,
  },
});
